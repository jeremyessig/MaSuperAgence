{% extends "base.html.twig" %}

{% block title 'Agence' %}

{% block body %}
<div class="jumbotron text-center">
    <h1>Bienvenue sur la homepage</h1>
    <p>Lorem ipsum, dolor sit amet consectetur adipisicing elit. Officiis consectetur nesciunt odio voluptatibus? Adipisci eaque sapiente nesciunt saepe, expedita illo eligendi optio placeat consectetur aperiam, similique quas voluptatum labore reprehenderit!</p>
</div>

<div class="container">
    <h2>Les derniers biens</h2>
    <div class="row felx">
    {% for property in properties %}
        <div class="col-3">
            <div class="card">
                <div class="card-body">
                    <h5 class="card-title">
                        <a href="{{ path('property.show', {id: property.id, slug: property.slug}) }}">{{ property.title }}</a>
                    </h5>
                    <p class="card-text">{{ property.city }} {{ property.postalCode }}</p>
                    <div class="text-primary"> {{property.formattedPrice}}€ </div>
                </div>
            </div>
        </div>
    {% endfor %}
    </div>
</div>
{% endblock %}
